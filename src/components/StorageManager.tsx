"use client";
import {useEffect,useState} from "react";
type Stats={outputsBytes:number;trashBytes:number;outputPath:string};
const format=(bytes:number)=>bytes<1024?`${bytes} B`:bytes<1024*1024?`${(bytes/1024).toFixed(1)} KB`:`${(bytes/1024/1024).toFixed(1)} MB`;
export default function StorageManager(){const [stats,setStats]=useState<Stats|null>(null),[error,setError]=useState("");async function refresh(){const response=await fetch("/api/storage",{cache:"no-store"}),data=await response.json();if(!response.ok)throw new Error(data.error);setStats(data)}useEffect(()=>{void refresh().catch(e=>setError(e.message))},[]);async function empty(){if(!confirm("确认永久清空回收站？此操作无法恢复。"))return;setError("");try{const response=await fetch("/api/storage",{method:"DELETE"}),data=await response.json();if(!response.ok)throw new Error(data.error);setStats(data)}catch(e){setError(e instanceof Error?e.message:"清空失败")}}return <section className="card" style={{marginTop:14}}><div className="panel-head"><div><h2>本地文件与回收站</h2><small>生成结果长期保存在服务器输出目录</small></div><button className="danger" disabled={!stats?.trashBytes} onClick={empty}>清空回收站</button></div>{error&&<div className="error">{error}</div>}<div className="grid"><article className="image-card"><h3>项目图片占用</h3><strong>{stats?format(stats.outputsBytes):"读取中…"}</strong></article><article className="image-card"><h3>回收站占用</h3><strong>{stats?format(stats.trashBytes):"读取中…"}</strong></article><article className="image-card"><h3>保存位置</h3><small className="path-text">{stats?.outputPath||"读取中…"}</small></article></div></section>}
