import {NextResponse} from "next/server";import {enqueueWorkflow} from "@/lib/job-runner";import {z} from "zod";
const schema=z.object({projectId:z.string().uuid(),sourceImage:z.string().startsWith("/api/files/"),poseReferenceImages:z.array(z.string().startsWith("/api/files/")).length(3),referenceMode:z.enum(["library","upload"]),mode:z.enum(["fast","standard","quality"]),shotType:z.enum(["上半身","下半身","全身"]),face:z.boolean(),background:z.boolean(),detailRequirements:z.string().min(1).max(2000),poseInstructions:z.array(z.string().min(1).max(500)).length(3).optional(),focus:z.enum(["upper","lower"]).optional(),slot:z.number().int().min(1).max(3).optional(),modelPreference:z.enum(["primary","fallback"]).optional(),idempotencyKey:z.string().max(200).optional()});
export async function POST(request:Request){try{const value=schema.parse(await request.json()),operation=await enqueueWorkflow("pose",value,value.idempotencyKey);return NextResponse.json({jobId:operation.id,status:operation.status},{status:202})}catch(error){return NextResponse.json({error:error instanceof Error?error.message:"姿势生成失败"},{status:400})}}
