import {NextResponse} from "next/server";import {readOutput} from "@/lib/ai/storage";export async function GET(_:Request,{params}:{params:Promise<{path:string[]}>}){try{const parts=(await params).path;const b=await readOutput(parts);const ext=parts.at(-1)?.split(".").at(-1);return new NextResponse(b,{headers:{"Content-Type":ext==="png"?"image/png":ext==="webp"?"image/webp":"image/jpeg","Cache-Control":"private, no-store, max-age=0","X-Content-Type-Options":"nosniff"}})}catch{return NextResponse.json({error:"图片不存在或路径非法"},{status:404})}}
