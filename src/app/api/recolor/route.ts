import {NextResponse} from "next/server";import {enqueueWorkflow} from "@/lib/job-runner";import {z} from "zod";
const image=z.string().startsWith("/api/files/");const schema=z.object({projectId:z.string().uuid(),colorReferenceImage:image.optional(),colorReferenceCrop:image.optional(),targetColorId:z.string().uuid().optional(),colorName:z.string().min(1).max(60),hexColor:z.string().max(7).refine(value=>!value||/^#[0-9A-Fa-f]{6}$/.test(value),"颜色色号必须是 6 位 HEX，例如 #C8A06A"),garmentArea:z.enum(["上衣","裤子","裙子","整套服装"]),protectedAreas:z.array(z.string()).max(20),extraRequirements:z.string().max(2000),mode:z.enum(["fast","standard","quality"]),poseImages:z.array(image).length(3).optional(),slot:z.number().int().min(1).max(3).optional(),modelPreference:z.enum(["primary","fallback"]).optional()}).refine(value=>Boolean(value.colorReferenceImage||value.colorReferenceCrop||value.hexColor),{message:"请上传颜色参考图、框选参考颜色，或选择/输入 HEX 色号"});
export async function POST(request:Request){try{const operation=await enqueueWorkflow("recolor",schema.parse(await request.json()));return NextResponse.json({jobId:operation.id,status:operation.status},{status:202})}catch(error){return NextResponse.json({error:error instanceof Error?error.message:"复色失败"},{status:400})}}
